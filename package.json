{
  "name": "eventstream-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome DevTools extension for inspecting and formatting SSE events",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "demo": "node demo/server.mjs"
  }
}
